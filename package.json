{
  "name": "hgpt",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "deploy": "turbo run build --scope=web --no-deps --include-dependencies"
  },
  "devDependencies": {
    "pnpm": "^7.31.0",
    "prettier": "^2.8.7",
    "turbo": "latest",
    "typescript": "^5.0.4"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.15.0",
  "dependencies": {
    "eslint": "^8.38.0"
  }
}
